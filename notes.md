# 작업 인수인계 노트

## 현재 목표

이 프로젝트는 초등 5학년 사회 수업용 `인구 불균형 선택 게임`이다. 현재 핵심 목표는 단순한 문제 카드형 게임을 넘어서, 학생이 `수도권`과 `지방`의 이익과 감수할 점을 비교하며 선택하는 시나리오형 게임으로 다듬는 것이다.

특히 사용자가 지적한 문제는 다음과 같다.

- 지문, 조건 카드, 선택지 이익/감수 문구가 서로 다른 조건을 암시한다.
- 조건 카드가 무엇을 의미하는지 설명이 부족하다.
- 이미 수도권/지방을 선택했는데 다음 라운드에서도 새로 취업하는 것처럼 보이는 문구가 있었다.
- `crisis`, `opportunity`가 현재 거주지와 맞지 않는 장면을 보여줄 수 있었다.
- 선택 기록이 다음 장면에 충분히 이어지지 않아 선택 게임 느낌이 약했다.

## 지금까지 한 일

### 1. 선택 기록 기반 시나리오 레이어 추가

[app.js](app.js)에 선택 기록을 바탕으로 장면을 조합하는 템플릿을 추가했다.

관련 코드:

- `transitionTemplates`
- `roundNarrativeTemplates`
- `getNarrativeContext(round)`

이제 각 라운드 화면에 아래 정보가 표시된다.

```text
선택 기록
지난 선택으로 이어진 현재 상황
이번 라운드에서 고민해야 할 주제
```

예:

```text
선택 기록: 1R 수도권 → 2R 수도권
수도권 생활을 이어 가기로 했습니다...
이번에는 병원과 문화시설 접근성을 비교합니다...
```

### 2. 같은 지역 유지와 지역 이동 문구 분리

기존에는 수도권이면 매번 `취업 확정`, 지방이면 매번 `취업 확률`처럼 표시됐다. 이 때문에 이미 수도권에서 취업했는데 다음 라운드에도 다시 취업하는 것처럼 보였다.

수정한 함수:

- `getChoiceContext(type)`
- `getIncomeText(type, option)`
- `getStabilityText(type, option)`
- `getDiceGuideText(type, option)`

현재는 다음처럼 구분된다.

```text
수도권에 계속 살기 -> 기존 일자리 유지
수도권으로 이동하기 -> 수도권 일자리 확보
지방에 계속 살기 -> 지역 수입 안정 또는 이번 달 수입 확보 확률
지방으로 이동하기 -> 지방 이동 후 취업 확률
```

### 3. 선택지 이익/감수 문구 개선

선택지 카드에 `이익`과 `감수`를 표시하도록 추가했다.

관련 코드:

- `getChoiceTradeoff(type, option, round)`
- `renderChoiceButton(type, option, icon, round)`

처음에는 일반 문구라서 1라운드처럼 `일자리/월세` 문항인데 `병원/문화시설 접근`을 암시하는 문제가 있었다. 이후 `round.coreFactors` 기준으로만 이익/감수 문구를 만들도록 수정했다.

### 4. 조건 카드 설명 추가

조건 카드가 단어만 보여서 학생이 의미를 이해하기 어려웠다.

추가한 객체:

- `factorDescriptions`

이제 카드에 아래처럼 짧은 설명이 붙는다.

```text
일자리: 수입을 얻을 기회
집값·월세: 매달 내는 주거비
의료: 병원과 진료 접근성
문화: 여가와 지역 활동
```

관련 스타일도 [styles.css](styles.css)에 추가했다.

### 5. 질문 문구 명확화

기존:

```text
이번 상황과 관련 깊은 조건 2개를 고르세요.
```

수정:

```text
이번 {라운드 태그} 상황에서 지역 선택을 결정하는 핵심 조건 2개를 고르세요.
```

관련 함수:

- `getPuzzleQuestion(round)`

목적은 조건 카드가 “좋아 보이는 장점”이 아니라 “지역 선택을 판단하는 핵심 원인”임을 분명히 하는 것이다.

### 6. 위기/기회 장면의 지역 불일치 일부 수정

`crisis`, `opportunity`는 현재 거주지보다 우선해서 뜬다. 그래서 수도권에 계속 살았는데 지방 마을 공동체 같은 문장이 나올 수 있었다.

추가한 객체:

- `sceneVariants`

수도권/지방 문장을 분리한 케이스:

- 6R 위기 `느린 정책`
- 7R 기회 `지역 창업`
- 8R 위기 `생활 기반 축소`
- 8R 기회 `마을 공동체`

관련 함수:

- `getDisplayScene(round)`

### 7. 검증 문서 작성

검증 기준 문서:

- [SCENARIO_AUDIT_GUIDELINES.md](SCENARIO_AUDIT_GUIDELINES.md)

주요 기준:

```text
지문에서 말한 핵심 갈등
= 조건 카드의 근거
= 핵심 조건 2개
= 수도권/지방 선택지의 이익과 감수
```

감사 결과 문서:

- [SCENARIO_AUDIT.md](SCENARIO_AUDIT.md)

이 문서는 현재 49개 화면 상태를 기준으로 `카드 근거 약함`, `핵심 조건 근거 약함` 등을 표로 정리한다.

### 8. 시나리오 캡처 생성

검증용 스크린샷 폴더:

- [tmp_screenshots](tmp_screenshots)

구성:

```text
1R 공통 1개
2~9R의 수도권 기본, 지방 기본, 위기 수도권, 위기 지방, 기회 수도권, 기회 지방
총 49개 화면
```

주의:

- 이 폴더는 검증 산출물이다.
- 안의 `pages/*.html`은 캡처용 임시 페이지다.
- 사용자가 현재 이 파일들을 열어 보고 있다.

## 현재 남은 문제

1차 수정으로 `선택지 이익/감수 문구가 엉뚱한 조건을 암시하는 문제`는 크게 줄었다.

하지만 아직 중요한 문제가 남아 있다.

```text
조건 카드 4개 중 일부가 지문 안에서 직접 근거가 약하다.
```

예를 들어 1라운드:

```text
지문 핵심: 일자리 증가, 월세 상승
조건 카드: 일자리, 집값·월세, 의료, 문화
핵심 조건: 일자리, 집값·월세
문제: 의료/문화 카드가 왜 후보인지 지문에서 설명되지 않는다.
```

사용자가 원하는 것은 이런 논리적 비약을 전부 잡아내고 수정하는 것이다.

## 앞으로 해야 할 일

### 1. `SCENARIO_AUDIT.md` 기준으로 문항 데이터 정비

대상은 [app.js](app.js)의 `rounds` 배열이다.

각 시나리오마다 아래를 확인한다.

```text
지문 핵심
조건 카드 4개
핵심 조건 2개
수도권 선택 이익/감수
지방 선택 이익/감수
결과 문장
```

판단 기준:

```text
카드가 맞다 -> 지문에 그 카드 단서를 추가
카드가 안 맞다 -> 조건 카드를 더 적절한 카드로 교체
```

### 2. 우선순위가 높은 수정

먼저 아래 유형을 고친다.

- 지문에 전혀 없는 후보 카드
- 핵심 조건이 지문에서 약하게만 보이는 경우
- 선택지는 특정 조건을 강조하는데 조건 카드와 안 맞는 경우
- 위기/기회가 현재 거주지와 어색한 경우

### 3. 1라운드 수정 방향

현재 1라운드는 `의료`, `문화` 카드가 약하다.

추천 수정 방향:

```text
조건 카드: 일자리, 집값·월세, 물가, 가족
핵심 조건: 일자리, 집값·월세
```

또는 `의료/문화`를 유지하려면 지문에 병원/문화시설 접근성 단서를 추가해야 한다. 하지만 1라운드의 본래 갈등은 첫 정착의 `일자리 vs 월세`이므로 카드 교체가 더 자연스럽다.

### 4. 수정 후 재검증

수정 후 해야 할 것:

```powershell
node --check app.js
```

그리고 가능하면 다시 49개 화면을 캡처해서 확인한다. 현재 캡처 생성 스크립트는 임시 파일로 만들었다가 삭제했으므로, 필요하면 다시 만들어야 한다.

## 작업할 때 주의할 점

- `README.md`와 `index.html`은 일부 인코딩이 깨져 보인다. 현재 주요 한글 데이터는 `app.js` 기준으로 정상이다.
- `tmp_screenshots/pages/*.html`은 생성된 검증 페이지라 직접 수정 대상이 아니다.
- 실제 게임 데이터는 [app.js](app.js)의 `rounds` 배열과 관련 helper 함수가 중심이다.
- 조건 카드 문구를 바꾸면 `coreFactors`도 반드시 함께 확인해야 한다.
- 선택지 결과 문장도 “이미 같은 지역에 사는지 / 새로 이동하는지” 논리에 맞는지 확인해야 한다.

## 현재 git 상태상 주요 변경 파일

```text
app.js
styles.css
BRANCH_CASES.md
SCENARIO_AUDIT.md
SCENARIO_AUDIT_GUIDELINES.md
notes.md
tmp_screenshots/
```

## 추천 다음 작업 순서

1. `SCENARIO_AUDIT.md`를 열어 수정 필요 항목을 라운드 순서대로 확인한다.
2. 1R부터 `rounds` 데이터의 `factors`, `coreFactors`, `scene`, 결과 문장을 정리한다.
3. 2R~9R도 같은 방식으로 진행한다.
4. `node --check app.js`를 실행한다.
5. 실제 화면 또는 재생성한 캡처로 조건 카드와 선택지 이익/감수가 같은 논리를 가리키는지 확인한다.
